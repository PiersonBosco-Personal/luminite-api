<?php


// Pest global function stubs for Intellephense
// This file is not executed — it just satisfies the IDE

if (false) {
    function uses(string ...$classNames): \Pest\PendingCalls\UsesCall
    {
        // stub
    }

    function it(string $description, \Closure $closure = null): void
    {
        // stub
    }

    function test(string $description, \Closure $closure = null): void
    {
        // stub
    }

    function beforeEach(\Closure $closure = null): void {}
    function afterEach(\Closure $closure = null): void {}
    function beforeAll(\Closure $closure = null): void {}
    function afterAll(\Closure $closure = null): void {}
    function expect(mixed $value = null): \Pest\Expectation {}
}
